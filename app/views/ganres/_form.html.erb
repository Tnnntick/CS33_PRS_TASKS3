<%= form_with(model: ganre) do |form| %>
  <% if ganre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ganre.errors.count, "error") %> prohibited this ganre from being saved:</h2>

      <ul>
        <% ganre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
